<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Secrets</title>
  <style>
    /* ===== page reset & font ===== */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    /* ===== body & container ===== */
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background: linear-gradient(135deg, #dbeafe, #93c5fd);
      padding: 20px;
    }
    .container {
      background: #fff;
      width: 100%;
      max-width: 500px;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      transition: transform 0.3s;
    }
    .container:hover {
      transform: translateY(-5px);
    }

    /* ===== heading ===== */
    h1 {
      color: #1e3a8a;
      font-size: 28px;
      margin-bottom: 24px;
      text-align: center;
    }

    /* ===== task list ===== */
    .task-list {
      list-style: none;
      max-height: 300px;
      overflow-y: auto;
      border: 1px solid #e5e7eb;
      border-radius: 8px;
      background: #f8fafc;
      margin-bottom: 24px;
      padding: 0;
    }
    .task-item {
      display: flex;
      align-items: center;
      padding: 12px 16px;
      border-bottom: 1px solid #e2e8f0;
      transition: background 0.2s;
    }
    .task-item:last-child {
      border-bottom: none;
    }
    .task-item:hover {
      background: #eef2f7;
    }
    .task-item .serial {
      font-weight: 600;
      color: #2563eb;
      margin-right: 12px;
    }
    .task-item .description {
      flex: 1;
      color: #334155;
    }

    /* ===== add-task form ===== */
    .add-task-form {
      display: flex;
      gap: 8px;
      margin-bottom: 16px;
    }
    .add-task-form input {
      flex: 1;
      padding: 12px;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
      transition: border-color 0.3s, box-shadow 0.3s;
    }
    .add-task-form input:focus {
      outline: none;
      border-color: #3b82f6;
      box-shadow: 0 0 5px rgba(59,130,246,0.4);
    }
    .auth-button {
      padding: 12px 20px;
      background: #3b82f6;
      color: #fff;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: 600;
      transition: background 0.3s;
    }
    .auth-button:hover {
      background: #2563eb;
    }

    /* ===== logout ===== */
    .logout-form {
      text-align: center;
    }
    .logout-form button {
      margin-top: 16px;
      background: #ef4444;
    }
    .logout-form button:hover {
      background: #dc2626;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Your Tasks</h1>

    <ul class="task-list">
      <% (user.tasks || []).forEach(task => { %>
        <li class="task-item">
          <span class="serial"><%= task.serial %>.</span>
          <span class="description"><%= task.description %></span>
        </li>
      <% }) %>
    </ul>

    <form class="add-task-form" action="/tasks/add" method="post">
      <input name="description" type="text" placeholder="New task…" required>
      <button type="submit" class="auth-button">Add Task</button>
    </form>

    <form class="logout-form" action="/logout" method="post">
      <button type="submit" class="auth-button">Logout</button>
    </form>
  </div>

</body>
</html>
